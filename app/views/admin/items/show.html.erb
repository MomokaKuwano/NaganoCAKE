<h3>商品詳細</h3>

  <%= image_tag @item.get_image %>
  <p>商品名</p>
    <%= @item.name %>
  <p>商品説明</p>
    <%= @item.introduction %>
  <p>ジャンル</p>
    <%= @item.genre.name %>
  <p>税抜価格</br>(税込価格)</p>
    <%= @item.price.to_s(:delimited)%>(<%= @item.add_tax_price.to_s(:delimited) %>)
  <p>ステータス</p>
    <% if @item.is_active %>
      <p>販売中</p>
    <% else %>
      <p>販売停止中</p>
    <% end %>

  <%= link_to "編集する", edit_admin_item_path(@item.id) %>