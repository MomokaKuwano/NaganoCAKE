<h3>注文情報確認</h3>


    <table>
      <thead>
        <tr>
          <th>商品名</th>
          <th>単価(税込)</th>
          <th>数量</th>
          <th>小計</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
          <% @cart_items.each do |cart_item|%>
        <tr>
          <td>
            <%= cart_item.name %>
          </td>
          <td>
            <%= cart_item.add_tax_price.to_s(:delimited)  %>
          </td>
          <td>
           
          </td>
          <td>
          </td>
        
        </tr>
        <% end %>
      </tbody>
    </table>

    送料
    商品合計
    請求金額

    <h4>支払い方法</h4>
      <%= @order.payment_method %>
    <h4>お届け先</h4>
      <%= @order.postal_code %>
      <%= @order.address %>
      <%= @order.name %>